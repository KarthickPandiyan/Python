#Given 2 set of array validate the elements are present in the given list, also check the it is in sequence if so retrun True.

#declaring the list of Integer
complete = [2,2,5,6,-1,4]
check = [6,-1,4]
match=[]
for i in range(len(complete)):

    for j in range(len(check)):

        if(complete[i] == check[j]):
            match.append(complete[i])
if(match == check):
    print("True")
else:
    print("False")
